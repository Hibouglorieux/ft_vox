#ifndef BIOME_CLASS_H
# define BIOME_CLASS_H

# define MAX_TEMPERATURE 75 // Celcius
# define WATER_LEVEL 63

enum biomeType {
	BasicBiome = 0,
    ForestBiome,
    DesertBiome,
    MountainBiome
};

class BiomeManager : public Object{
public:

private:
	
};

#endif
